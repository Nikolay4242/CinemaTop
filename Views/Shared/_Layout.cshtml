<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CinemaTop</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <style>
        body {
    background: url("@Url.Content("~/img/cinema.webp")") no-repeat center center fixed;
    background-size: cover;
    color: #f1f1f1;
}

        body {
            background-color: #121212;
            color: #f1f1f1;
        }
        header {
            background-color: #1f1f1f;
            padding: 1rem 0;
            margin-bottom: 2rem;
        }
        header h1 {
            color: #f1c40f;
            margin: 0;
            font-size: 2rem;
        }
        nav a {
            color: #f1f1f1;
            margin: 0 15px;
            text-decoration: none;
            font-weight: bold;
        }
        nav a:hover {
            color: #f1c40f;
        }
        .container {
            max-width: 1200px;
            margin: auto;
            padding: 20px;
        }
        footer {
            text-align: center;
            color: gray;
            font-size: 0.9rem;
            margin-top: 40px;
        }
    </style>
</head>
<body>
    <header class="text-center">
        <h1>🎬 CinemaTop</h1>
        <nav class="mt-2">
            <a href="/">Главная</a>
            <a href="/Movies">Фильмы</a>
        </nav>
    </header>
    <div class="container">
        @RenderBody()
    </div>
    <footer>
        &copy; @DateTime.Now.Year – CinemaTop. Все права защищены.
    </footer>
</body>
</html>
